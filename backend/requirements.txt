# Azure Functions Core
azure-functions==1.20.0

# FastAPI and dependencies
fastapi==0.109.2
python-multipart==0.0.9
pydantic==2.6.1
pydantic-settings==2.1.0
email-validator==2.1.0.post1

# ASGI server (not needed for Azure Functions but useful for local testing)
uvicorn[standard]==0.27.1

# Additional utilities
python-dotenv==1.0.1

# Cloudflare R2 / S3-compatible storage
boto3==1.34.34

# Testing utilities
requests==2.31.0

# Azure SQL Server connection
# pyodbc — used on Azure Functions Linux (ODBC Driver 18 pre-installed)
pyodbc==5.1.0
# pymssql — used for local Windows development (no driver install needed)
pymssql==2.2.11

# SQLAlchemy ORM
SQLAlchemy==2.0.30

# Async HTTP client for external Bible API
httpx==0.27.0
